# Graduation-project

The code in this `main` branch is currently in its initial stage

to keep the repository lightweight, large files such as YOLO model weights, and `.zip` files are not included here

all large files are available in the Releases section

:)
